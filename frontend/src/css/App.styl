html, 
body {
    height: 100%;
    box-sizing: border-box;
}

body {
    margin: 0 auto;
    padding: 0;    
    font-family: sans-serif;
    width: 100%;
}

.App {
    position: relative;
    width: 100%;
    height: 100vh;
    display: flex;
    flex-direction: row;
    margin: 0 auto;
}

* {
    box-sizing: border-box;
}

.line {
    width: 100%;
    height: 5px;
    background-color: #1b51da;
    
}